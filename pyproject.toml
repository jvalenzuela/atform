[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[project]
name = "testgen"
version = "0.0.0"
description = "Test procedure generator."
requires-python = ">= 3.11"
dependencies = [
    "reportlab>=4.2.0",
]
